# Vendored PLib components

ONITalk vendors the `PLibCore`, `PLibUI`, and `PLibOptions` source components from
Peter Han's ONIMods repository, version 4.25.0.0, under the MIT license in
`LICENSE`.

Local change: `PTextField` includes a password field type so API credentials are
masked while they are entered in the ONITalk options dialog.
