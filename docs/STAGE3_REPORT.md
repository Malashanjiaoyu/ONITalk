# 阶段 3：常驻聊天历史窗口

日期：2026-06-28  
版本：0.3.0

## 已完成

- 新增独立的 `DialogueMessage` 与有界 `DialogueHistory`。
- 聊天历史不与模型的短期配对记忆混用。
- 新台词在主线程同时进入头顶气泡、日志和聊天窗口。
- 窗口挂载到游戏 Overlay Canvas，不暂停模拟。
- 实现消息滚动、自动定位最新消息、拖动、缩放、最小化和清空。
- 窗口位置与尺寸按父画布比例保存，并限制在屏幕范围内。
- 增加窗口显示、历史上限、字体大小和不透明度选项。
- 公开变化统一维护在仓库根目录 `CHANGELOG.md`。

## 自动验证

- Core 检查：19/19 通过。
- Release 构建：0 警告，0 错误。
- 示例配置 JSON 解析成功。
- 构建产物版本：0.3.0。

## 游戏验证

- 真实存档窗口创建成功，日志记录 `Chat history window attached`。
- 实时追加、换行和中文字体显示正常。
- 拖动、缩放、最小化、清空和滚轮通过验证。
- 布局保存正常；实测尺寸和透明度已写回配置。
- 背景透明度、悬停加深与屏幕范围约束通过验证。

## 实测修订

- 窗口创建和中文台词实时追加已验证成功。
- 修复缩放角与消息行同时挂载多个 Unity Graphic 导致的创建失败。
- 根据实测移除时间戳，并修正背景透明层重复叠加。
