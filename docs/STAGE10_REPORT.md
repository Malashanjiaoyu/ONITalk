# 阶段 10：本地化与提示词配置

状态：已完成；0.9.9 集中游戏内验收通过。

## 0.9.1 已完成

- 接入《缺氧》原生 `Localization` 与 `LocString`，不建立并行 UI 翻译系统。
- 游戏完成语言初始化后，从模组 `translations` 目录加载当前语言文件。
- 英语内置为稳定源文本与最终回退；首批提供完整简体中文翻译。
- 标准选项、服务商配置面板、聊天窗口、记忆工具、运行时结果和 Core 摘要迁移为 191 个稳定键。
- 选项页最顶部新增界面语言下拉框，支持跟随游戏、英语、简体中文和西班牙语。
- Release 构建和本地部署产物均包含翻译目录。
- 自动检查模板与翻译之间的缺键、重复键、空值、过期源文本、未知键和格式变量。

## 当前验证

- 本地化覆盖：202/202 个键，简体中文和西班牙语通过。
- Core：67/67 项检查通过。
- Release：0 警告、0 错误。
- 游戏内验证通过：选项页顶部已出现界面语言下拉框。
- `Player.log` 确认 `requested=zh`、`applied=zh`、`entries=79`，样例值为“启用 ONITalk”。
- 本轮日志没有 ONITalk 本地化异常；现有翻译警告来自其他翻译模组处理的游戏本体文本。
- 0.9.4 西语联动验证通过：`requested=es`、`applied=es`、`dialogueLanguage=Español`，DeepSeek 实际生成西班牙语台词。
- 0.9.5 新增受保护的默认提示词、自定义风格模板、变量校验、恢复、复制默认和不调用 API 的最终预览。
- 提示词预览及默认提示词复制自动跟随 ONITalk 当前实际界面语言。
- 0.9.6 修复自定义提示词复选框未按 PLib 点击前状态推进、导致无法勾选的问题。
- 0.9.7 运行时聊天、记忆预览和记忆库窗口按当前字符串表解析文本，并支持在保存语言选项后刷新聊天窗口。
- 0.9.8 将提示词面板、服务商面板、API 验证结果也接入当前语言表，并增加运行时本地化防回归检查。
- 0.9.9 将长期记忆确认弹窗提升到最高排序层，并让四个记忆注入预设随英语、简中和西语切换。
- 2026-07-06 集中验收通过：长期记忆确认弹窗置顶正常，四个记忆预设随语言切换，英语与西语运行时标题均由日志确认正确解析。

## 下一批

1. 处理已存行动/事件内容的显示语言。
2. 完成跨存档隔离、错误回退和 DLC 发布前技术回归。
