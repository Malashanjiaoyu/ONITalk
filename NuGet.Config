<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Phase 0 has no PackageReference dependencies, so builds stay offline. -->
  <packageSources>
    <clear />
  </packageSources>
</configuration>
